version: "3.9"

services:
  frontend:
    build: ./2025_2_MindLeak_frontend
    container_name: frontend
    ports:
      - "80:3000"    # На сервере будет доступно по http://<ip>
    restart: always
