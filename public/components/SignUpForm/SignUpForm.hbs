<div class="registration-modal">
    <div class="registration-modal__content">
        <img src="/img/LogoMain.svg" alt="Логотип" class="registration-modal__logo" />
        <h1>Регистрация</h1>

        <form class="registration-form__body">
            <div class="form-group">
                <label for="username">Название аккаунта</label>
                {{> input type="text" placeholder="AnyAccountName" name="username"}}
            </div>

            <div class="form-group">
                <label for="email">Почта</label>
                {{> input type="email" placeholder="example@mail.xyz" name="email"}}
            </div>

            <div class="form-group">
                <label for="password">Пароль</label>
                <div class="input-wrapper">
                    {{> input type="password" placeholder="**********" name="password"}}
                    <span class="password-toggle" aria-hidden="true">🙉</span>
                </div>
            </div>

            <div class="form-group">
                <label for="confirmPassword">Подтверждение пароля</label>
                <div class="input-wrapper">
                    {{> input type="password" placeholder="**********" name="confirmPassword"}}
                    <span class="password-toggle" aria-hidden="true">🙉</span>
                </div>
            </div>

            <div class="button-container">
                {{> button text="Зарегистрироваться" key="signUp" type="submit"}}
            </div>

            <p class="registration-form__footer">
                Есть аккаунт? <a href="/login" class="link">Войти</a>
            </p>
        </form>
    </div>
</div>