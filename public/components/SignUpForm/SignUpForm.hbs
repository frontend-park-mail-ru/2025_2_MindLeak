<div class="registration-modal">
    <div class="registration-modal__content">
        <img src="/img/LogoMain.svg" alt="Логотип" class="registration-modal__logo" />
        <h1 class="registration-modal__title">Регистрация</h1>

        <form class="registration-form__body">
            <div class="form__group">
                <label for="username" class="form__label">Название аккаунта</label>
                {{> input type="text" placeholder="Название аккаунта" name="username"}}
            </div>

            <div class="form__group">
                <label for="email" class="form__label">Почта</label>
                {{> input type="email" placeholder="example@mail.xyz" name="email"}}
            </div>

            <div class="form__group">
                <label for="password" class="form__label">Пароль</label>
                <div class="form__input-wrapper">
                    {{> input type="password" placeholder="пароль" name="password"}}
                    <span class="form__password-toggle" aria-hidden="true">🙉</span>
                </div>
            </div>

            <div class="form__group">
                <label for="confirmPassword" class="form__label">Подтверждение пароля</label>
                <div class="form__input-wrapper">
                    {{> input type="password" placeholder="пароль" name="confirmPassword"}}
                    <span class="form__password-toggle" aria-hidden="true">🙉</span>
                </div>
            </div>

            <div class="button__container">
                {{> button text="Зарегистрироваться" key="signUp" type="submit"}}
            </div>

            <p class="registration-form__footer">
                Есть аккаунт? <a href="/login" class="link" data-router-ignore>Войти</a>
            </p>
        </form>
    </div>
</div>